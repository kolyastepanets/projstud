var $answer = $('#answer-' + <%= @answer.id %>);
var $answer_body = $answer.children('.answer-stuff');
$answer_body.children('.answer-body').text('<%= @answer.body %>');

$('.edit-answer-link').show();
$('.edit_answer').hide();

$(document).ready(ready)
$(document).on('page:load', ready)